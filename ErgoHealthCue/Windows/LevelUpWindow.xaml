<Window x:Class="ErgoHealthCue.Windows.LevelUpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Level Up!" 
        Height="300" Width="400"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    
    <Border BorderBrush="#F59E0B" 
            BorderThickness="4" 
            CornerRadius="20"
            Background="White"
            Padding="40">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.4" Color="#F59E0B"/>
        </Border.Effect>
        
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- Celebration Icon -->
            <TextBlock Text="🎉" 
                      FontSize="60"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,20"/>
            
            <!-- Level Up Text -->
            <TextBlock Text="LEVEL UP!"
                      FontSize="36"
                      FontWeight="Bold"
                      Foreground="#F59E0B"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,10"/>
            
            <!-- Level Number -->
            <TextBlock x:Name="LevelText"
                      Text="Level 5"
                      FontSize="48"
                      FontWeight="Bold"
                      Foreground="#1F2937"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,20"/>
            
            <!-- Message -->
            <TextBlock Text="Keep up the great work!"
                      FontSize="18"
                      Foreground="#6B7280"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,25"/>
            
            <!-- Close Button -->
            <Button Content="Continue"
                   Width="140"
                   Height="44"
                   FontSize="16"
                   FontWeight="Medium"
                   Click="CloseButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#F59E0B"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                           CornerRadius="10"
                                           Padding="20,10">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
                                        </Border.Effect>
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D97706"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Border>
</Window>
