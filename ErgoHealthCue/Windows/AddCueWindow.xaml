<Window x:Class="ErgoHealthCue.Windows.AddCueWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add New Cue" 
        Height="300" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Cue Type:" Margin="0,0,0,5"/>
        <ComboBox Grid.Row="1" x:Name="TypeComboBox" Height="30" Margin="0,0,0,15"/>
        
        <TextBlock Grid.Row="2" Text="Title:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="3" x:Name="TitleTextBox" Height="30" Margin="0,0,0,15"/>
        
        <TextBlock Grid.Row="4" Text="Description:" Margin="0,0,0,5" VerticalAlignment="Top"/>
        <TextBox Grid.Row="4" x:Name="DescriptionTextBox" 
                 TextWrapping="Wrap" 
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,30,0,15"/>
        
        <StackPanel Grid.Row="5" 
                   Orientation="Horizontal" 
                   HorizontalAlignment="Right">
            <Button x:Name="OkButton"
                   Content="Add"
                   Width="80"
                   Height="30"
                   Margin="0,0,10,0"
                   Click="OkButton_Click"
                   IsDefault="True"/>
            <Button x:Name="CancelButton"
                   Content="Cancel"
                   Width="80"
                   Height="30"
                   Click="CancelButton_Click"
                   IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
