<Window x:Class="ErgoHealthCue.Windows.BadgeListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Badge Achievements" 
        Height="650" Width="800"
        WindowStartupLocation="CenterOwner"
        Background="#F9FAFB">
    
    <Window.Resources>
        <Style x:Key="BadgeCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
        
        <Style x:Key="BadgeCardUnlocked" TargetType="Border" BasedOn="{StaticResource BadgeCard}">
            <Setter Property="BorderBrush" Value="#F59E0B"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFFBEB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="BadgeCardLocked" TargetType="Border" BasedOn="{StaticResource BadgeCard}">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Opacity" Value="0.6"/>
        </Style>
        
        <Style x:Key="BadgeCardNegative" TargetType="Border" BasedOn="{StaticResource BadgeCard}">
            <Setter Property="BorderBrush" Value="#EF4444"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FEF2F2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="🏆 Badge Achievements" FontSize="32" FontWeight="Bold" Foreground="#111827"/>
            <TextBlock x:Name="SubtitleText" 
                       Text="0 / 113 badges unlocked" 
                       FontSize="14" 
                       Foreground="#6B7280" 
                       Margin="0,5,0,0"/>
        </StackPanel>
        
        <!-- Filter Tabs -->
        <Border Grid.Row="1" 
                Background="White" 
                BorderBrush="#E5E7EB" 
                BorderThickness="1" 
                CornerRadius="12" 
                Padding="15"
                Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <RadioButton x:Name="AllBadgesRadio" 
                            Content="All Badges" 
                            GroupName="BadgeFilter"
                            FontSize="14"
                            Margin="0,0,15,0"
                            IsChecked="True"
                            Checked="BadgeFilter_Checked"/>
                <RadioButton x:Name="PositiveBadgesRadio" 
                            Content="Achievement Badges" 
                            GroupName="BadgeFilter"
                            FontSize="14"
                            Margin="0,0,15,0"
                            Checked="BadgeFilter_Checked"/>
                <RadioButton x:Name="NegativeBadgesRadio" 
                            Content="Negative Badges" 
                            GroupName="BadgeFilter"
                            FontSize="14"
                            Margin="0,0,15,0"
                            Checked="BadgeFilter_Checked"/>
                <RadioButton x:Name="UnlockedOnlyRadio" 
                            Content="Unlocked Only" 
                            GroupName="BadgeFilter"
                            FontSize="14"
                            Checked="BadgeFilter_Checked"/>
            </StackPanel>
        </Border>
        
        <!-- Badge List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="BadgeItemsControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{Binding Style}" 
                                Width="230" 
                                Height="140"
                                ToolTip="{Binding Tooltip}">
                            <StackPanel>
                                <TextBlock Text="{Binding Icon}" 
                                          FontSize="32" 
                                          HorizontalAlignment="Center"
                                          Margin="0,0,0,8"/>
                                <TextBlock Text="{Binding Name}" 
                                          FontSize="16" 
                                          FontWeight="SemiBold"
                                          Foreground="{Binding NameColor}"
                                          HorizontalAlignment="Center"
                                          TextWrapping="Wrap"
                                          TextAlignment="Center"
                                          Margin="0,0,0,5"/>
                                <TextBlock Text="{Binding RequirementText}" 
                                          FontSize="12" 
                                          Foreground="{Binding RequirementColor}"
                                          HorizontalAlignment="Center"
                                          TextWrapping="Wrap"
                                          TextAlignment="Center"
                                          Visibility="{Binding RequirementVisibility}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        
        <!-- Close Button -->
        <Button Grid.Row="3"
                Content="Close"
                Width="120"
                Height="44"
                FontSize="15"
                HorizontalAlignment="Right"
                Margin="0,20,0,0"
                Click="CloseButton_Click">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background">
                        <Setter.Value>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#6366F1" Offset="0"/>
                                <GradientStop Color="#4F46E5" Offset="1"/>
                            </LinearGradientBrush>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                       CornerRadius="10"
                                       Padding="20,10">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.2" Color="#6366F1"/>
                                    </Border.Effect>
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                    VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#4F46E5" Offset="0"/>
                                        <GradientStop Color="#4338CA" Offset="1"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Window>
